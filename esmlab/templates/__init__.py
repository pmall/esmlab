"""Static HTML pages, two per report topic: ``<topic>_entry`` and ``<topic>_index``.

Named after the calls that fill them, :func:`~esmlab.sequences_render.render_entry` and
:func:`~esmlab.sequences_render.render_index`; :mod:`esmlab.rendering` is the
mechanism.
"""
